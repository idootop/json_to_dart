{
  "name": "json2class",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^7.0.0",
    "lint-staged": ">=10"
  },
  "lint-staged": {
    "*.{ts,tsx}": "npx eslint --cache --fix",
    "*.{css,sass,scss}": "npx stylelint --cache --fix",
    "*.{ts,tsx,css,sass,scss}": "npx prettier --write"
  },
  "workspaces": [
    "web"
  ]
}
